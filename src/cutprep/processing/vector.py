"""Vector (SVG/DXF) prep and validation — placeholder.

The vector validation pipeline (parse -> detect open paths -> merge overlaps ->
simplify curves) is the next feature to build.
"""

from __future__ import annotations
