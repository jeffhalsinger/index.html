# Copy to .env and adjust for your environment.
FLASK_SECRET_KEY=change-me
# Directory where uploaded files are stored (relative to the project root).
CUTPREP_UPLOAD_DIR=uploads
# Max upload size in bytes (default 16 MiB).
CUTPREP_MAX_CONTENT_LENGTH=16777216
