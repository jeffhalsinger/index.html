# Copy this file to `.env` for local testing, and set the same two values in the
# Vercel dashboard under Settings -> Environment Variables for the real deploy.
#
# These keys live ONLY on the server. They are never sent to the phone.

ANTHROPIC_API_KEY=sk-ant-your-key-here
YOUTUBE_API_KEY=your-google-api-key-here
